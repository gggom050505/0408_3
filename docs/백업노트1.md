# 백업노트1 (재현 런북)

이 문서는 **현재 저장소 상태를 다시 재현**하기 위한 체크리스트입니다.  
기획 배경은 `docs/MAKING_NOTES.md`를 참고하고, 이 문서는 실행/검증 순서만 다룹니다.

최종 정리: **2026-04-29**

---

## 1) 실행 전제

- 기본 모드: 오프라인 번들
- 권장 실행:
  - `flutter run --dart-define=GGGOM_OFFLINE_BUNDLE=true`
- 웹 자산 확인(선택):
  - `flutter run -d chrome --dart-define=GGGOM_OFFLINE_BUNDLE=true --dart-define=ASSET_ORIGIN=<https://...>`

---

## 2) 핵심 기능 재현 순서

### STEP 1. 로그인/홈 진입
1. 앱 실행 후 로그인(신규/기존/로컬 계정).
2. 홈 화면 및 GNB 탭 노출 확인.

### STEP 2. 오프닝 스플래시
1. 앱 시작 시 `assets/opening/` 이미지 캐러셀 노출 확인.
2. 점 인디케이터가 이미지 개수와 맞게 표시되는지 확인.
3. 터치 시 다음 단계로 넘어가는지 확인.

관련 파일:
- `lib/config/opening_assets.dart`
- `lib/widgets/splash_screen.dart`
- `assets/opening/open(1).png` ~ `open(3).png`

### STEP 3. 간지 달력
1. 간지 달력 탭에서 날짜 이동/선택 확인.
2. 상단 **오행 안내 배너** 펼침 확인.
3. 날짜 셀 탭 시 바텀시트가 열리고 일진/연간/월간 패턴이 보이는지 확인.

관련 파일:
- `lib/widgets/ganji_calendar_tab.dart`
- `lib/data/ganji_sixty_day_patterns.dart`
- `lib/data/five_elements_guide.dart`

### STEP 4. 타로 탭
1. 카드 설명 보기 스위치를 켜기/끄기.
2. 앱 재실행 후 같은 값으로 유지되는지 확인.

관련 파일:
- `lib/widgets/tarot_tab.dart`
- `lib/standalone/local_app_preferences.dart`

### STEP 5. 오늘의 타로
1. 10칸 슬롯 채우기.
2. **한 번에 뒤집기** 버튼 노출/동작 확인.
3. 카드 설명 보기 스위치 켜기/끄기 후 재실행 유지 확인.
4. 완료 후 결과/게시 흐름 정상 확인.

관련 파일:
- `lib/widgets/today_tarot_screen.dart`
- `lib/standalone/local_app_preferences.dart`

---

## 3) 빠른 점검 커맨드

- 정적 분석:
  - `dart analyze lib`
- 핵심 테스트:
  - `flutter test test/today_tarot_screen_test.dart`
  - `flutter test test/making_notes_screen_test.dart`

---

## 4) 수정 포인트 인덱스

- 실행/부팅: `lib/main.dart`, `lib/widgets/app_root.dart`
- 홈/탭 허브: `lib/widgets/home_screen.dart`, `lib/widgets/gnb.dart`
- 오프닝: `lib/widgets/splash_screen.dart`, `lib/config/opening_assets.dart`
- 간지 달력: `lib/widgets/ganji_calendar_tab.dart`, `lib/data/ganji_sixty_day_patterns.dart`, `lib/data/five_elements_guide.dart`
- 타로/오늘의 타로:
  - `lib/widgets/tarot_tab.dart`
  - `lib/widgets/today_tarot_screen.dart`
  - `lib/standalone/local_app_preferences.dart`

---

## 5) 메모

- 역사 사건명(예: 임진왜란, 갑오개혁 등)은 보통 **연간 기준**이므로, 간지 달력의 일진 해설은 “유형 참고” 성격입니다.
- 빌드 산출물(`_live_main.dart.js`)과 개인 메모성 문서는 배포 커밋에서 분리 관리 권장.
